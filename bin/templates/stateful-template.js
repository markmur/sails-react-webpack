import React, { Component } from 'react';

class $NAME extends Component {
  constructor(props) {
    super(props);

    this.state = {
    };
  }

  render() {
    return (
      <div className="$NAME">
        $NAME
      </div>
    );
  }
}

$NAME.defaultProps = {
};

$NAME.propTypes = {
};

export default $NAME;
